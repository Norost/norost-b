[package]
name = "virtio_gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
pci = { path = "../pci/" }
virtio = { path = "../virtio" }
volatile = { path = "../volatile" }

[dependencies.endian]
package = "nora_endian"
version = "0.1"
