[package]
name = "norostb_kernel"
version = "0.2.0"
edition = "2021"

[dependencies]
# When built as part of libstd
core = { version = "1.0.0", optional = true, package = "rustc-std-workspace-core" }

[features]
rustc-dep-of-std = [
	"core",
]
