cargo-features = ["per-package-target"]

[package]
name = "jail"
version = "0.1.0"
edition = "2021"
default-target = "x86_64-unknown-norostb"

[dependencies]
norostb_rt = { path = "../../lib/rust/rt" }
norostb_rt_alloc = { path = "../../lib/rust/rt_alloc" }
