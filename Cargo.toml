cargo-features = ["strip"]

[workspace]

members = [
	"kernel",
	"boot/amd64",
	"drivers/virtio_block",
	"lib/rust/endian",
	"lib/rust/pci",
	"lib/rust/virtio",
	"lib/rust/volatile",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = "fat"
#strip = "symbols"

[profile.release.package.noraboot]
codegen-units = 1
opt-level = 'z'
#strip = "symbols"
